apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis-mcx.fullname" . }}-api
  labels:
    {{- include "aegis-mcx.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "aegis-mcx.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
